/**
 * The small mark in the admin nav header, beside the collection list.
 * Sized to sit on Payload's own baseline rather than fill the space.
 */
export function Icon() {
  return (
    <img
      src="/icons/favicon-96x96.png"
      alt=""
      width={28}
      height={28}
      style={{ display: 'block' }}
    />
  )
}
