# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
# Broad on purpose: .env backups and per-machine variants hold real
# credentials, and the earlier `.env` / `.env*.local` pair missed things
# like `.env.backup-before-s3`.
.env*
!.env.example

# Common ignores
node_modules
.next/
.DS_Store

# TypeScript / Next generated
*.tsbuildinfo
next-env.d.ts

# Payload
/public/media